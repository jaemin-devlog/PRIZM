Mobile delivery record

Primary transport
The mobile gateway negotiated HTTP/2 in production and recorded the selected protocol.

Fallback transport
A retired fallback endpoint remained on HTTP/1.1 during the migration window.

Offline queue
The offline queue used NimbusCache to retain submitted work until connectivity returned.

Unshipped prototype
A separate prototype mentioned ZephyrDBX but was never connected to the mobile gateway.
