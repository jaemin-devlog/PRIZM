# PRZ-018 — 문서 상세 미리보기 페이지 Tasks

> **현재 상태:** `COMPLETED`

## P1. 페이지 전환과 상태

- [x] `T1` 현재 모달·경로·thumbnail·PDF API 계약을 확인했다.
- [x] `T2` 상세 보기를 URL 기반 인페이지 화면으로 전환한다.
- [x] `T3` 목록 복귀와 브라우저 history 동기화를 검증한다.

## P2. 미리보기와 버전

- [x] `T4` 선택 버전의 PDF thumbnail과 TXT·오류 대체 화면을 구현한다.
- [x] `T5` 오른쪽 버전 선택·상태·등록·삭제 UI를 구현한다.

## P3. 문서 관리와 반응형 UI

- [x] `T6` 정보·태그·문서 삭제 기능을 새 화면에 배치한다.
- [x] `T7` 데스크톱·태블릿·모바일 layout을 구현한다.

## P4. 검증과 감사

- [x] `T8` frontend unit test·lint·build를 통과한다.
- [x] `T9` 브라우저 동작과 반응형 화면을 확인한다.
- [x] `T10` 최종 diff와 보존 계약을 감사하고 Evidence를 기록한다.

## P5. 상세 화면 밀도 보정

- [x] `T11` 기본 정보와 태그를 하나의 전체 너비 카드에 세로로 배치한다.
- [x] `T12` PDF thumbnail과 미리보기 캔버스를 약 2/3 크기로 조정한다.
- [x] `T13` frontend 검사, 브라우저 확인과 Docker 재반영을 완료한다.
