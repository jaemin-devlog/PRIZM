# PRZ-022 Plan

1. 현재 기준선과 기존 Evidence를 감사하고 네 축을 분류한다.
2. 평가 계약과 반복 횟수를 결과 실행 전에 동결한다.
3. Production 코드를 바꾸지 않고 재현 가능한 평가 test/script와 `results/`의 원시 JSON을 만든다.
4. focused 검증 뒤 전체 backend unit·PostgreSQL integration과 Linux filesystem 검증을 실행한다.
5. 현재 상태 문서와 문제 해결 사례에는 확인된 사실만 요약하고 Evidence를 연결한다.
6. diff·secret·artifact·문서 링크를 확인하고 통합 작업 없이 종료한다.
